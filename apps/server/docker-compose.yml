services:
  planme-server:
    build: .
    ports:
      - "3847:3847"
    volumes:
      - planme-data:/app/data
    environment:
      - PLANME_PORT=3847
    restart: unless-stopped

volumes:
  planme-data:
